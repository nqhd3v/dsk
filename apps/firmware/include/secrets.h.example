// Copy to include/secrets.h and fill in real values.
// include/secrets.h is gitignored — never commit credentials.
//
// Phase 0–5: this file is unused (standalone firmware).
// Phase 6+ (ESP↔RPi link): provide WiFi + MQTT creds here for bench dev,
// later replaced by BLE provisioning into NVS.

#pragma once

// WiFi STA — RPi AP "DG-<mac>"
#define DG_WIFI_SSID     "DG-bench"
#define DG_WIFI_PASSWORD "changeme"

// MQTT broker on RPi
#define DG_MQTT_HOST     "192.168.4.1"
#define DG_MQTT_PORT     1883
#define DG_MQTT_USER     "node1"
#define DG_MQTT_PASSWORD "changeme"

// Node identity (later derived from chip ID if unset)
#define DG_NODE_ID       "node1"
